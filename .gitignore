# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
dist-server/

# NOTE: dist/ is intentionally committed — Vercel serves the prerendered
# dist/ as-is (build-smart.mjs skips building on Vercel). Do not gitignore it,
# or rebuilt asset/image files silently won't deploy.
